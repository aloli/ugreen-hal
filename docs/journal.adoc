= Journal du projet — ugreen-hal
:toc:

Ce journal consigne, par ordre chronologique, les décisions structurantes et les étapes franchies. Le détail technique des relevés matériels vit dans `hardware-notes.adoc` et `protocol-i2c-leds.adoc` ; ce document se concentre sur le fil du projet.

== 2026-07-15 — Cadrage initial

* Décision de cibler FreeBSD + ZFS plutôt que Linux, en rejetant explicitement TrueNAS SCALE (passé sous Linux).
* Recherche de projets FreeBSD/ZFS existants à rejoindre plutôt que de repartir de zéro : identification de *zVault* (fork communautaire de TrueNAS CORE, FreeBSD + ZFS + middleware/webUI dérivés de TrueNAS) comme cible de contribution privilégiée, avec XigmaNAS gardé comme solution de repli.
* Choix du NAS de test : *UGREEN NASync DXP2800* (Intel N100, 2 baies), préféré au DXP4800 Plus pour son support matériel plus simple (pas de contrôleur réseau Aquantia AQC113, dont le pilote FreeBSD est immature).
* Nommage du dépôt en `ugreen-hal` pour signaler un scope volontairement étroit (portage matériel), mergeable dans zVault, plutôt qu'un firmware concurrent.
* Classification du projet comme projet personnel plutôt que projet d'entreprise.

== 2026-07-15 — Sprint 0 : matériel complémentaire

* Identification du besoin en matériel de test : adaptateur USB-Ethernet de secours, support USB/SSD externe, disque de sauvegarde externe.
* Constat de l'absence de réseau filaire au poste de travail (WiFi uniquement) : recherche d'un pont WiFi-vers-Ethernet.
* Choix retenu : GL.iNet GL-SFT1200 (« Opal ») en mode Extender pour le pont réseau.
* Évaluation de plusieurs solutions d'accès distant à l'écran du NAS (JetKVM, GL.iNet Comet GL-RM1, GL.iNet Comet Pro GL-RM10) ; achat du *GL.iNet Comet (GL-RM1)*, retenu pour son rapport prix/fonctionnalités (4K@30fps, Tailscale) malgré l'absence de WiFi intégré — compensée par le pont GL-SFT1200 déjà prévu.
* Commande passée : livraison estimée à 5 jours.

== 2026-07-15 — Premier relevé matériel (photos console HDMI)

* Première série de photos du démarrage UGOS analysée : confirmation que la sortie HDMI expose bien la console noyau/systemd (pas seulement une interface applicative), validant a posteriori l'intérêt du KVM.
* Relevés initiaux : identification DMI (DXP2800 V1.2 251215), réseau (`igc`, famille Intel I225/I226), stockage interne eMMC, contrôleur LED (chip ID `0xc5b2`, adresse SMBus `0x3a`, module `leds-mcu`), boutons (`ug-gpiokey`, GPIO), supervision matérielle (ITE IT8613).
* Détail complet consigné dans `hardware-notes.adoc`.

== 2026-07-15 — Second relevé matériel (reproductibilité)

* Seconde série de photos analysée : chip ID I2C, adresse SMBus, chip watchdog et pilote boutons tous identiques au premier relevé — *reproductibilité confirmée*.
* Informations complémentaires obtenues : chaîne de démarrage (GRUB 2.06, entrée unique `UGOSPRO-NAS`, splash UGREEN avant le menu), pilote `r8152` chargé au démarrage (même famille de puce que l'adaptateur USB-Ethernet de secours déjà retenu).

== 2026-07-15 — Documentation complète du protocole LED (pendant l'attente de livraison)

* Clonage et lecture du code source de référence `miskcoo/ugreen_leds_controller` (implémentation espace utilisateur, protocole déjà éprouvé sur DXP2800 selon le README amont).
* Protocole entièrement documenté : bus SMBus I801, adresse esclave `0x3a`, format des blocs de lecture (11 octets, registre `0x81+ID`) et d'écriture (12 octets, registre `0x00+ID`), table des 5 sous-commandes (luminosité, couleur, on/off, clignotement, respiration), calcul de somme de contrôle.
* Correction importante par rapport à une hypothèse initiale : la cible de portage FreeBSD la plus probable est la pile `smbus(4)`/`ichsmb(4)` (SMBus intégré au PCH Intel), pas `iic(4)` seul — cohérent avec l'usage exclusif de transactions SMBus block dans le code de référence.
* Squelette de code C de portage FreeBSD rédigé (non testé, en attente de matériel).

== 2026-07-15 — Dépôt public, canal de contact zVault et procédure de démarrage

* Confirmation du choix `aloli` (plutôt que `papilip`) pour héberger ce dépôt : décision délibérée, avec l'intention de communiquer sur le projet en cas de succès.
* Dépôt `ugreen-hal` créé sur GitHub sous `github.com/aloli/ugreen-hal`, en lecture publique (licence BSD-2-Clause), premier commit poussé sur la branche `development`.
* Recherche des modalités officielles de contact avec l'équipe zVault : canal privilégié identifié comme `github.com/zvaultio/Community` (issues/discussions), en complément de l'adresse `team@zvault.io`. Brouillon de message de prise de contact rédigé (voir plan de projet).
* Découverte d'un épisode de gouvernance significatif, jusqu'ici seulement anticipé comme risque théorique : peu après le lancement de zVault (février 2025), iXsystems a adressé une notification de retrait pour code propriétaire, entraînant le retrait temporaire des ISO ; l'équipe a réécrit les composants concernés et republié une version propre en mai 2025 (`13.3-MASTER-202505042329`). Le projet est actif et a démontré sa capacité à corriger le tir, mais confirme la nécessité de garder XigmaNAS comme option de repli documentée.
* Procédure officielle de préparation de la clé USB d'installation documentée (wiki `zVault-Installation`) : téléchargement de l'ISO depuis la page de releases GitHub, écriture via `dd` sous macOS (`diskutil list` pour identifier le périphérique, puis `sudo dd if=... of=/dev/diskN bs=1M conv=sync`). Procédure intégrée à `scripts/prepare-usb-zvault.sh` sous forme de guide commenté (pas d'automatisation, geste destructeur à exécuter à la main).
* Point encore ouvert : la touche exacte du menu de démarrage BIOS du DXP2800 (probable Suppr/F2 pour le setup, F7/F11/Échap pour le menu de boot, non documentée officiellement) — à confirmer par observation directe au premier essai.
* Précision terminologique : la « session Claude Code » mentionnée dans le plan de projet désigne en réalité cette session Claude (Cowork) ; pas de bascule nécessaire vers l'outil en ligne de commande du même nom pour la suite du travail.

== 2026-07-18 — Documentation bilingue, commit poussé, matériel confirmé

* README scindé en anglais (`README.adoc`) et français (`README.fr.adoc`), avec renvoi croisé entre les deux ; section « Auteur » ajoutée sur chacun.
* Copyright de la `LICENSE` mis à jour : Philippe Nénert (ALOLI sas).
* Commit `01e204e` (« Bilingual README, zVault install docs, contact channel research ») créé et poussé sur `github.com/aloli/ugreen-hal`, branche `development`.
* Correction de la configuration Git locale (`user.name` contenait l'e-mail en double).
* Sauvegarde du NAS de test jugée non nécessaire : le NAS est neuf, sans disque installé, donc aucune donnée à perdre. `scripts/backup-nas.sh` reste disponible pour un usage ultérieur (avant la phase 4, remplacement du firmware interne).
* Switch KVM HDMI 4 entrées commandé : TESmart HKS401-P23-V2 (4K@60Hz, émulation EDID confirmée, USB 3.0). Anticipe le sprint 5 (généralisation multi-modèles) — permettra de partager un même écran/clavier/Comet entre plusieurs NAS UGREEN.
* Seule action du sprint 0 encore ouverte : publier l'issue de prise de contact sur `github.com/zvaultio/Community`.

== 2026-07-19 — Décision stratégique : double cible d'intégration (zVault + XigmaNAS)

* Décision : `ugreen-hal` vise désormais zVault *et* XigmaNAS comme cibles d'intégration à part entière, plutôt qu'une cible principale et un repli théorique. Motivation : les deux projets sont des forks/distributions FreeBSD + ZFS actifs et légitimes, et la contribution aux deux communautés est jugée valoir l'effort supplémentaire.
* Architecture retenue pour ne pas doubler le travail : `src/cli/` reste un cœur strictement FreeBSD standard (`smbus(4)`/`gpio(4)`), indépendant de toute distribution — c'est la partie la plus longue et la plus risquée (rétro-ingénierie, tests sur matériel réel), qui ne se fait qu'une seule fois. Deux couches fines d'empaquetage viendront ensuite se greffer dessus : `integrations/zvault/` (module `zvio-os`/`zvio-middleware`) et `integrations/xigmanas/` (suivant le modèle d'extension déjà établi par la communauté XigmaNAS — contributeur JRGTH, schéma `etc/` + `gui/` PHP + script d'init + fichier `version`).
* Ordre de développement confirmé : pilote validé sur zVault en premier (sprints 3 à 5), puis portage vers XigmaNAS (nouveau sprint 5bis), plutôt que les deux intégrations en parallèle dès le sprint 3.
* Canal de contact XigmaNAS identifié pour une future prise de contact : ticket sur `sourceforge.net/p/xigmanas/tickets/` (compte SourceForge gratuit, pas besoin du forum officiel fermé) — volontairement non urgent, à envoyer une fois le pilote plus avancé, après la démarche zVault.
* Plan de projet mis à jour en v1.8 (architecture Phase 3, sprints 5/5bis/6, sections zVault/XigmaNAS).

== 2026-07-19 — Catalogue NAS UGREEN, décisions d'achat, bascule vers Claude Code

* *Catalogue matériel* : création de `docs/catalogue-nas-ugreen.adoc`, comparant tous les NAS UGREEN vendus en France (DH2300, DH4300 Plus, DXP2800, DXP2800 GT, DXP4800, DXP4800 GT, DXP4800 Plus, DXP4800 Pro, DXP6800 Pro, DXP8800 Plus, DXP480T Plus) : baies, CPU, RAM, M.2, réseau, USB. Document en orientation paysage (`:pdf-page-layout: landscape`) pour l'export PDF.
* *DH2300/DH4300 Plus confirmés hors scope* : SoC ARM Rockchip RK3576, sans support FreeBSD (vérifié sur les notes matérielles officielles FreeBSD 15.1 — seuls RK3328/RK3399/RK3566/RK3568 sont listés — et sur le fil de forum communautaire Rockchip 35xx, actif sur RK3566/RK3568 mais sans aucune mention du RK3576).
* *Risque Aquantia AQC113 (10GbE) reconfirmé toujours d'actualité* : le port FreeBSD tiers `net/aquantia-atlantic-kmod` reste en « Development Preview », dernière mise à jour le 05/10/2025, sans mainteneur assigné. Ce risque touche `DXP4800 Plus/Pro`, `DXP6800 Pro`, `DXP8800 Plus`, `DXP480T Plus`, et les deux modèles « GT » (chipset 10GbE non documenté publiquement pour ces derniers, à traiter comme inconnu).
* *Second NAS de test (pour XigmaNAS) : décision revue en cours de route*. Recommandation initiale (`DXP4800` de base, tout-2,5GbE) invalidée : modèle confirmé épuisé (boutique officielle France et revendeur Galaxus). Alternatives retenues, par ordre de prudence : un second `DXP2800` (identique, zéro risque, confirmé en stock à 329,99-369,99 €) ou un `DXP4800 Plus` (4 baies, confirmé en stock à 619,99-653,00 €, viable en n'utilisant que son port 2,5GbE et en ignorant son port 10GbE tant que le pilote Aquantia n'est pas mûr). Choix final entre les deux laissé à l'utilisateur.
* *Modèles AMD (`DXP2800 GT`, `DXP4800 GT`) découverts et documentés* : plateforme AMD Ryzen Embedded R2514 (RAM ECC, U.2 sur les baies SATA), confirmée disponible en France sur `nas-fr.ugreen.com`. Analyse : excellents candidats pour la Phase 5 (généralisation du pilote LED à une plateforme non-Intel, chip ID potentiellement différent de `0xc5b2`), le `DXP2800 GT` étant le plus rigoureux des deux (même nombre de baies — 2 — que le DXP2800 actuel, isolant la seule variable plateforme) et le moins cher. Volontairement écartés du second achat immédiat : ne pas cumuler l'inconnue XigmaNAS et l'inconnue d'une nouvelle plateforme matérielle en même temps.
* *Recommandations matériel pour le DXP2800 actuel* : RAM (8 Go DDR5, un seul slot SO-DIMM, max 16 Go) — pas d'urgence, à trancher une fois un vrai pool ZFS en charge (ARC) plutôt que par précaution. M.2 NVMe (2 emplacements 2280, PCIe 3.0, utilisables comme vrai vdev ZFS sous FreeBSD, pas seulement comme cache) — utile à prévoir avant le sprint 5 (tests de résilience ZFS) mais pas bloquant maintenant. Disques SATA — plus prioritaires que RAM/M.2 pour l'objectif central du projet : les 2 baies SATA sont celles associées aux LED de façade à piloter ; deux disques SATA 2,5" (SSD de préférence, cycles insertion/retrait plus rapides et reproductibles), même petits ou d'occasion, suffisent pour tester le comportement réel des LED par baie.
* *Bascule vers l'outil en ligne de commande Claude Code* : cette session (Claude/Cowork) ne transmettant pas sa mémoire à une nouvelle session Claude Code, tout le contexte utile a été explicitement couché sur disque : le plan de projet lui-même a été committé dans le dépôt pour la première fois (`docs/plan-firmware-freebsd-ugreen-nas.adoc`, v1.9 — jusqu'ici seulement envoyé en téléchargement, jamais versionné), cette entrée de journal, et un `CLAUDE.md` minimal à la racine pointant vers les documents AsciiDoc de référence.

== 2026-07-19 — Première session Claude Code (en local)

* *Reprise du contexte réussie sans mémoire de la session Cowork* : le pari du commit `af03069` (plan committé + `CLAUDE.md` + journal à jour) est validé — la totalité du contexte du projet a pu être reconstituée par simple lecture du dépôt, sans avoir à recoller quoi que ce soit dans la conversation. La consigne « tout ce qui doit survivre est couché sur disque » est donc à maintenir à chaque fin de session.
* *Affichage HDMI verbeux : question tranchée*. La question restée ouverte dans le plan (avait-il fallu une combinaison de touches ou un bouton particulier pour obtenir le journal de démarrage complet sur l'écran HDMI ?) est résolue : aucune manipulation, simple appui sur le bouton d'alimentation. Le mode verbeux est le *comportement par défaut* du DXP2800, pas un mode déclenché. La console de démarrage sera donc visible à chaque essai sans geste préalable — c'est le principal canal de diagnostic des sprints 2 et 3.
* *CI corrigée* : le workflow `.github/workflows/ci.yml` ne se déclenchait que sur `push` vers `main` et sur les pull requests. Or il n'existe pas de branche `main` dans ce dépôt (le travail se fait par principe sur `development`) : la validation AsciiDoc n'avait donc jamais tourné une seule fois depuis la création du dépôt. Déclencheur corrigé vers `development`.
* *Première exécution de la CI : échec immédiat, et instructif*. Dès le premier déclenchement réel, l'étape `prepare` a échoué sur `pkg: No packages available to install matching 'asciidoctor'`. Cause : Asciidoctor est une gem Ruby, empaquetée sous FreeBSD dans `textproc/rubygem-asciidoctor` — le nom `asciidoctor` n'existe pas dans les dépôts. Personne n'avait pu le constater puisque le workflow n'avait jamais tourné. Le reste de l'infrastructure est en revanche validé : la VM FreeBSD 15.1 démarre, `rsync` synchronise les sources, les dépôts de paquets se mettent à jour (37 978 paquets).
* *Deux défauts connexes corrigés dans la foulée*. (1) `find docs -name '*.adoc' -exec asciidoctor ... \;` ne faisait jamais échouer le job : `find` ignore le code de retour de `-exec`, si bien qu'un document invalide aurait été annoncé « OK ». Remplacé par une boucle `for` sous `set -e`. (2) La validation ne couvrait que `docs/` (5 fichiers) et ignorait les README et `CONTRIBUTING.adoc` ; elle porte désormais sur les 10 documents du dépôt.
* *Validation locale via un outil maison* : `asciidoctor` n'étant pas installé sur le Mac, les documents ont été validés avec `crystal-asciidoctor` (portage Crystal d'Asciidoctor 2.0.26, dépôt `aloli-crystal`) — les 10 fichiers passent le parse et la conversion HTML5. À noter pour un usage ultérieur : la bibliothèque fonctionne, mais sa cible CLI (`src/cli.cr`) ne compile pas (`Asciidoctor.send(:create_converter, ...)`, idiome Ruby resté au portage) ; il faut passer par l'API `Asciidoctor.load`/`Asciidoctor.convert`.
* *Point d'état Git* : neuf commits locaux d'avance sur `origin/development`, et aucune branche amont configurée pour `development` (un `git push` nu échoue). Une branche locale `production` subsiste par ailleurs, restée sur le commit initial `9f561a7`, jamais poussée — résidu sans usage, sort à trancher.

== 2026-07-20 — État de l'art, matériel reçu, sauvegarde d'UGOS redevenue prioritaire

* *Personne n'a porté zVault ni XigmaNAS sur du matériel UGREEN* — vérifié plutôt que supposé. Recherche `is:issue UGREEN` sur `zvaultio/Community` : zéro résultat sur 27 issues (aucune mention de NASync, DXP ou LED). Côté XigmaNAS, aucune trace d'UGREEN dans le wiki matériel ni dans les extensions communautaires. L'issue de prise de contact du sprint 0 reste donc entièrement originale, ce qu'elle devait précisément permettre de confirmer avant envoi.
* *Un autre utilisateur travaille sur le contrôleur LED sous FreeBSD* : fil « NASync LED controller » du forum FreeBSD (https://forums.freebsd.org/threads/nasync-led-controller.100889/), ouvert le 21/12/2025 sur FreeBSD 14.3-RELEASE. Ce n'est pas un projet concurrent — pas de dépôt, pas de code publié, pas de distribution NAS visée — mais une source précieuse : son auteur est resté bloqué cinq semaines sur un piège que nous aurions rencontré à l'identique.
* *Correction majeure du protocole LED, avant même le premier essai* : Linux exprime les adresses I2C sur 7 bits, FreeBSD sur 8 bits. L'adresse `0x3a` relevée au sprint 0 ne désigne donc rien sous FreeBSD ; la bonne valeur est `0x74` (`0x3a << 1`). `docs/protocol-i2c-leds.adoc` et son squelette C sont corrigés en conséquence. Deux réserves associées, issues du même fil : sous FreeBSD le contrôleur apparaît comme « Intel Alder Lake SMBus controller » et non comme `I801`, et le choix entre `iic(4)` et `smbus(4)` reste à déterminer par observation plutôt qu'à supposer.
* *Touche du BIOS identifiée* : `Ctrl-F12` (et non Suppr/F2/F7/F11), ce qui clôt une question ouverte depuis le 15/07 — sous réserve de confirmation au premier démarrage.
* *Piège du watchdog matériel* : l'IT8613 est actif au niveau du BIOS et redémarre la machine au bout de 20 minutes si aucun système ne le nourrit. UGOS s'en charge par un service dédié ; un système alternatif qui l'ignore subira des redémarrages spontanés, aisément confondus avec une instabilité matérielle. À désactiver dans le BIOS avant toute installation.
* *TrueNAS CORE 13.3-BETA2 tourne déjà sur un DXP6800* d'après un retour d'utilisateur (aux côtés d'OmniOS/napp-it). CORE 13.3 étant la base dont zVault est le fork, c'est le meilleur signal disponible pour les sprints 2 et 5 : le socle FreeBSD/ZFS démarre sur cette gamme, seuls les périphériques propriétaires manquent. Les LED sont en revanche confirmées inertes sous système alternatif sans pilote dédié.
* *Matériel reçu* : les clés USB (support d'installation) et le disque externe (support d'exécution persistant, pour éviter d'user une clé USB par les écritures continues). Le pont réseau GL-SFT1200 et le KVM Comet GL-RM1 ne sont pas encore arrivés — sans conséquence bloquante : l'écran HDMI et le clavier USB branchés directement sur le NAS suffisent pour le premier démarrage externe, le réseau ne devenant nécessaire que pour le SSH quotidien.
* *Décision révisée — la sauvegarde d'UGOS redevient recommandée*. Le 18/07, elle avait été jugée non nécessaire au motif que le NAS est neuf et sans disque : ce raisonnement ne portait que sur les *données utilisateur* et laissait de côté le firmware lui-même. `scripts/backup-nas.sh`, jusqu'ici un squelette partant sur une mauvaise méthode (copie en SSH depuis UGOS en fonctionnement, qui produirait une image incohérente), a été réécrit en procédure hors ligne depuis un live Linux.
+
[NOTE]
====
*Affirmation corrigée le même jour, après contestation justifiée de l'utilisateur.* Une première version de cette entrée affirmait qu'« UGREEN ne fournit aucune image de restauration ». C'est faux, ou du moins trop catégorique : UGREEN publie bien un « System Firmware » par modèle sur `ai.ugreen.com/pages/downloads`, au format `.img`, décrit par au moins un utilisateur comme remplaçant le système en place plutôt que comme un simple correctif applicatif.

Ce qui reste incertain, et maintient le clone au rang de précaution raisonnable : la page officielle en parle comme d'une *mise à jour* (« Use online update unless you specifically need a local firmware package ») ; aucun rapport public consulté ne confirme une restauration réussie depuis un eMMC réellement effacé ; plusieurs sources indiquent qu'en cas de corruption totale le fichier est fourni par le support et lié au numéro de série ; enfin l'eMMC contient des partitions propres à l'exemplaire (`UGREEN-SERVICE`, `USER-DATA`) et les partitions de démarrage `boot0`/`boot1`, qu'une image générique de modèle ne reproduit pas nécessairement.

Leçon de méthode : l'affirmation initiale reposait sur un unique message de forum, transformé en fait établi — précisément ce que `CONTRIBUTING.adoc` proscrit. Le statut correct est « probablement récupérable sans clone, mais personne ne l'a vérifié ».
====
* *Portée du clone* : il ne conditionne que la phase 4 (écriture sur le stockage interne). Les sprints 2 et 3, en démarrage externe sur clé USB ou SSD, n'écrivent rien en interne et ne sont pas concernés.
* *Filet de sécurité supplémentaire, relevé par l'utilisateur* : le second NAS, déjà prévu au plan pour l'intégration XigmaNAS, fait aussi office de *donneur d'eMMC*. Un exemplaire identique resté intact permet de cloner son stockage interne vers une machine dont le firmware aurait été perdu. Le repli ne coûte donc rien de plus que ce qui est déjà budgété, ce qui rend le risque de « brick » nettement moins critique qu'il n'apparaissait — et justifie de ne pas retarder les sprints pour cette raison.
+
[NOTE]
====
Réserve à vérifier avant de compter dessus : l'eMMC contient des partitions propres à l'exemplaire (`UGREEN-SERVICE`, `USER-DATA`) et le nom d'hôte par défaut dérive d'un identifiant matériel (`DXP2800-2CFF`). Plusieurs sources indiquent par ailleurs qu'UGREEN lie ses fichiers de restauration au numéro de série de l'appareil. Un clone provenant d'une *autre* machine pourrait donc y transporter une identité qui n'est pas la sienne, avec des conséquences inconnues sur les mises à jour ou le support. À tester le jour venu, sur la machine sacrifiable, jamais dans l'urgence.
====

== 2026-07-20 — Sprint 0 terminé : issue de contact zVault publiée

* *Issue publiée* : https://github.com/zvaultio/Community/issues/37, « Hardware support for UGREEN NASync NAS devices — intent to contribute », ouverte le 20/07/2026 par le compte `papilip`. Sans étiquette ni réponse à ce jour.
* *Le sprint 0 est clos* : c'était sa dernière action bloquante. Le dépôt existe et est public, le contact zVault est établi, la sauvegarde a été requalifiée en précaution non bloquante, le matériel complémentaire est réuni. Reste à enchaîner sur les sprints 1 et 2.
* *Précision d'identité, sans conséquence pratique* : l'issue est publiée depuis le compte `papilip`, tandis que le dépôt est hébergé sous l'organisation `aloli` (choix délibéré du 15/07, motivé par l'intention de communiquer sur le projet). Le message pointe bien vers `github.com/aloli/ugreen-hal`, donc rien à corriger — simplement à savoir si un jour la cohérence d'identité publique devient un sujet.
* *Message rédigé en anglais*, contrairement au brouillon initial de juillet : les deux communautés visées échangent en anglais. Texte conservé dans `docs/prise-de-contact.adoc`, avec celui destiné à XigmaNAS, volontairement différé au-delà du sprint 3.
* *Ce que le message annonce, et ce qu'il n'annonce pas* : l'état réel est présenté sans le farder — protocole documenté à partir du code Linux de référence, mais pas encore validé sur matériel sous FreeBSD. Surestimer l'avancement se serait retourné contre nous dès la première question technique.
* *Préparation des clés USB outillée* : les deux guides commentés (`prepare-usb-zvault.sh`, `prepare-usb-systemrescue.sh`) sont remplacés par `scripts/prepare-usb.cr`, un programme Crystal interactif — sur demande de l'utilisateur, et cohérent avec ses habitudes de développement. Il déroule la procédure au lieu de la donner à recopier : choix de l'image, vérification SHA-256 (implémentation validée contre `shasum`), sélection du disque *parmi les seuls périphériques externes physiques* — le disque interne du Mac ne peut donc jamais apparaître —, confirmation par recopie de l'identifiant du disque plutôt qu'un simple « oui », écriture via `/dev/rdiskN`, éjection. Sans dépendance externe : bibliothèque standard uniquement, pas de `shard.yml` imposé à un dépôt qui reste un projet C/FreeBSD. Le programme couvre les *trois* images du projet — SystemRescue, zVault et XigmaNAS, cette dernière ajoutée pour le sprint 5bis — et propose le téléchargement automatique via `curl` (reprise sur coupure, délai d'expiration, URL toujours corrigeable à l'exécution car une adresse codée en dur se périme à chaque version). L'URL SystemRescue a été vérifiée fonctionnelle ; celles de zVault et XigmaNAS ne sont pas codées, leurs pages officielles étant seules fiables.
* *Versions vérifiées ce jour pour la préparation des clés USB* : SystemRescue 13.01 (06/06/2026, noyau LTS 6.18.34) pour le live Linux ; côté zVault, la release `13.3-MASTER-202505042329` de mai 2025 reste la plus récente publiée — *quatorze mois sans nouvelle version*. Le projet n'est pas abandonné pour autant (les dépôts peuvent vivre sans publication d'ISO), mais c'est exactement le signal que le plan demandait de surveiller au titre du risque de gouvernance. À réévaluer si la réponse à l'issue tarde.

== 2026-07-20 — Cadrage : UGOS n'a pas vocation à revenir

* *Décision explicite de l'utilisateur* : l'objectif est de *supprimer UGOS*, pas de cohabiter avec lui. Le NAS finira sous FreeBSD, et l'impossibilité éventuelle de revenir en arrière est acceptée.
* *Conséquence sur la stratégie de sauvegarde*, qui a occupé une bonne part de la journée : le clone de l'eMMC cesse d'être un prérequis de projet. Il ne conserve de valeur que comme assurance de revente ou de garantie, et comme filet en cas d'abandon du projet. Il reste recommandé — il coûte une soirée — mais il ne conditionne plus aucun sprint, et ne doit surtout pas servir de motif à retarder la phase 4.
* *Ce qui reste vrai malgré cette décision* : la procédure de `scripts/backup-nas.sh` garde son intérêt documentaire pour les futurs utilisateurs du projet, qui n'auront pas tous la même intention. Un guide de portage qui n'expliquerait pas comment repartir en arrière serait incomplet.

== Piste à explorer plus tard — usage du CPU inutilisé

Intention exprimée le 20/07/2026 : exploiter le CPU du NAS lorsqu'il ne sert pas au stockage. Hors périmètre des sprints 0 à 6, mais notée ici pour ne pas se perdre.

* *Le socle FreeBSD s'y prête nativement* : les jails offrent un cloisonnement léger, sans la surcharge d'une machine virtuelle, et zVault en hérite directement de TrueNAS CORE. `bhyve` reste disponible si une vraie VM devient nécessaire.
* *Piste la plus utile au projet lui-même* : monter le NAS en hôte *poudriere*, la ferme de compilation de paquets de FreeBSD. Cela répondrait à l'intention — du CPU qui travaille pendant les heures creuses — tout en produisant exactement ce dont les sprints 5 et 5bis auront besoin : des paquets `ugreen-hal` construits pour zVault et pour XigmaNAS, sur la même architecture que la cible.
* *Contrainte à ne pas sous-estimer* : sur un DXP2800, la ressource rare n'est pas le CPU mais la *mémoire*. Le N100 a quatre cœurs souvent oisifs, mais 8 Gio de RAM que l'ARC de ZFS consomme volontiers. Toute charge de calcul entrera en concurrence avec le cache de lecture, et donc avec les performances du NAS en tant que NAS. C'est l'argument le plus sérieux en faveur du passage à 16 Gio, bien plus que la charge de stockage elle-même.
* *Précision sur ce passage à 16 Gio* : ce n'est pas un ajout mais un *remplacement*. Le DXP2800 n'a qu'un seul slot SO-DIMM, occupé par la barrette de 8 Gio d'origine, et plafonne à 16 Gio — la barrette retirée devient un surplus inutilisable sur cette machine. Comptez une DDR5 SO-DIMM de 16 Gio, 40 à 50 €. Non urgent : les sprints 0 à 6 tiennent sans difficulté dans 8 Gio, la mémoire ne devenant contraignante que le jour où des jails ou un poudriere se disputeront l'ARC avec le service de fichiers. Note d'opportunité : la barrette et les emplacements M.2 sont tous deux à l'intérieur du boîtier, alors que les baies SATA sont accessibles en façade — autant n'ouvrir la machine qu'une fois.

== 2026-07-21 — Banc QEMU opérationnel : tout ce qui pouvait l'être hors matériel est validé

* *Le banc fonctionne de bout en bout* : `qemu/`, image FreeBSD 15.0 amd64, VM démarrée en émulation TCG, compilation de `src/cli/` et rapatriement du binaire. Trois obstacles levés en chemin — `mktemp` inutilisable selon la politique du répertoire temporaire (préparation déplacée dans `run/`), firmware UEFI monté en `-bios` alors qu'`edk2-x86_64-code.fd` est une image *pflash* (corrigé en deux périphériques pflash, code en lecture seule et magasin de variables inscriptible), et un `sudo` manquant sur `kldload`.
* *La compilation passe contre les véritables en-têtes FreeBSD*, sans le moindre avertissement en `-Wall -Wextra`. `struct smbcmd` et les ioctls `SMB_BWRITE`/`SMB_BREAD`, déduits de la lecture de `sys/dev/smbus/smb.h`, étaient exacts. Le binaire produit est un ELF FreeBSD 15.0 amd64, directement copiable sur le DXP2800 — aucun compilateur ne sera nécessaire sur le NAS.
* *Découverte : le SMBus est émulé*. La machine `q35` de QEMU expose un contrôleur ICH9 auquel `ichsmb(4)` s'attache — le pilote même que vise le code sur le DXP2800. Le banc permet donc bien plus qu'une compilation, ce que son README sous-estimait.
* *Piège du device node, qui aurait coûté du temps sur le matériel* : `ichsmb.ko` et `smbus.ko` chargés, contrôleur sain dans `dmesg`, et pourtant aucun `/dev/smb*`. Trois modules distincts interviennent et seul le dernier crée le device node — `smb(4)`. Le symptôme est trompeur : tout paraît fonctionnel, ce qui oriente le diagnostic vers le code plutôt que vers un module manquant. `sudo kldload smb` (ou `smb_load="YES"` dans `/boot/loader.conf`) résout la chose.
* *Le noyau valide la forme de nos ioctls*, et c'est le résultat le plus important de la journée. Avec `/dev/smb0` disponible, `SMB_BREAD` renvoie `ENXIO` — erreur émise par la couche SMBus *après* transmission sur le bus, faute d'acquittement. Une structure mal remplie aurait été rejetée en amont avec `EINVAL`, sans jamais atteindre le bus. La correction de l'ioctl et de la structure n'est donc plus une déduction de lecture d'en-tête : elle est confirmée par l'implémentation.
* *Grille de diagnostic établie* pour l'essai sur matériel (voir `protocol-i2c-leds.adoc`) : `ENXIO` accusera l'adresse, `EINVAL` accusera notre code, un succès sans réaction de la LED désignera l'octet de longueur du bloc SMBus. Sans cette grille, le réflexe devant un échec serait de suspecter le protocole — l'hypothèse la plus coûteuse à explorer.
* *Ce qui reste hors de portée du banc, définitivement* : le dialogue avec le microcontrôleur UGREEN. C'est une puce soudée sur la carte mère ; aucune émulation ne la reproduit. Le sprint 3.3 conserve donc tout son sens, mais il aborde le matériel avec une seule inconnue au lieu de quatre.

== 2026-07-28 — Premier contact matériel : BIOS relevé, watchdog identifié

* *Premier essai sur le DXP2800.* `Ctrl-F12` confirmé comme touche du menu de démarrage. Démarrage sur SystemRescue réussi jusqu'au prompt root, puis redémarrage spontané vers UGOS — le watchdog, non désactivé faute d'être passé par le Setup.
* *Watchdog localisé et chiffré* : `Advanced` → `WatchDog Settings`, `Watch Dog Control [Enabled]`, `Degree [Minute]`, `Timer 3` — soit 3 minutes armées dès l'allumage. Corrige l'estimation « dizaine de secondes » de la veille : le « moins d'une minute » ressenti venait des ~2 minutes de démarrage de SystemRescue, qui ne laissaient qu'une minute avant déclenchement.
* *BIOS entièrement cartographié* (voir `hardware-notes.adoc`) : Setup AMI Aptio, six onglets. Confirme au passage plusieurs relevés antérieurs — puce Super I/O `IT8613`, contrôleur `eMMC 5.1`, deux emplacements `M.2 PCIE`, N100, 8 Gio DDR5 à 4800 MHz.
* *Deux découvertes qui pèseront sur la suite* :
** *Secure Boot présent.* SystemRescue a démarré avec (chargeur signé), mais zVault ne le pourra pas — FreeBSD n'a pas de chargeur signé pour les clés Microsoft. À désactiver avant l'étape zVault, sous peine d'un échec de démarrage difficile à diagnostiquer.
** *Le contrôleur eMMC est désactivable dans le BIOS* (`Chipset` → `Onboard Devices`). Levier propre pour empêcher le démarrage d'UGOS en phase 4, sans toucher à son contenu.
* *Numéro de série* volontairement gardé hors du dépôt public : UGREEN lie ses fichiers de restauration au numéro de série, à conserver dans un relevé privé.
* *Prochain geste* : désactiver watchdog et Secure Boot dans le Setup, sauvegarder, puis reprendre les relevés `lsblk` / `lspci` / `i2cdetect` sans la contrainte de temps.

== 2026-07-28 (suite) — Watchdog désactivé, relevés SystemRescue

* *Watchdog désactivé dans le Setup* : session SystemRescue désormais stable, plus de redémarrage. La voie est libre pour les relevés.
* *eMMC confirmée au bit près* : `lsblk` donne la table taille/label complète (`kernel`, `factory`, `rootfs2`, `ugswap`, `UGREEN-SERVICE`, `USER-DATA`, `boot0`/`boot1`), identique aux relevés photographiques antérieurs.
* *Les deux SSD sont installés dans les baies* : `sda` et `sdb`, 223,6 Gio, `RM 0` (non amovibles = SATA interne). Les LED de baies deviennent donc testables, et les tests de retrait à chaud du sprint 5 sont possibles.
* *Contrôleur SMBus identifié* : `8086:54a3`, « Intel Corporation Alder Lake-N SMBus », à `00:1f.4`. Confirme le retour du forum FreeBSD et fixe le point de vérification pour zVault — `ichsmb(4)` doit connaître cet identifiant. Le banc QEMU (ICH9) ne couvre pas ce point.
* *i2cdetect absent* : SystemRescue (base Arch) ne fournit pas `i2c-tools` d'office. Modules `i2c-i801` et `i2c-dev` chargés sans erreur ; reste à installer le paquet (`pacman -Sy i2c-tools`), sous réserve de réseau, pour confirmer `0x3a` et tenter une lecture LED directe façon `miskcoo/ugreen_leds_controller` — validation du protocole sous Linux avant FreeBSD.

== 2026-07-29 — Réseau en service, premier scan i2c : le 0x3a répond

* *Réseau opérationnel* : pont GL.iNet Flint configuré en répéteur WISP, NAS câblé sur un port LAN, Mac rejoint le même WiFi pour rester sur le même sous-réseau (voir `docs/reseau-banc.adoc`). SSH établi vers SystemRescue.
* *Piège SSH résolu* : `ssh` échouait en « No route to host » malgré un `ping` réussi — non pas un problème de routage, mais le pare-feu `nftables` par défaut de l'image live qui rejetait le port 22. `nft flush ruleset` a suffi.
* *Piège `i2c-dev` résolu* : `i2c-i801` s'attachait bien au contrôleur (confirmé par `dmesg` : bus `i2c-11` créé sur `8086:54a3`), mais `i2c-dev` n'était pas chargé — d'où l'absence de `/dev/i2c-*` et un `i2cdetect -l` muet. Un `modprobe -v i2c-dev` explicite a réglé la question. Corrige au passage une inquiétude soulevée en cours de route : l'identifiant PCI `8086:54a3` (Alder Lake-N) est bien reconnu par ce noyau — pas de lacune du pilote Linux.
* *Le `0x3a` répond, confirmé par sonde active* : `i2cdetect -y -r 11` détecte un périphérique à l'adresse exacte documentée depuis le sprint 0. C'est la première vérification qui ne repose plus sur la relecture d'un journal de démarrage, mais sur une sonde réelle du bus, avec les outils Linux qui ont servi à rétro-documenter le protocole. Deux adresses supplémentaires répondent sur le même bus (`0x44`, `0x48`), non identifiées, sans lien connu avec les LED — à creuser plus tard.
* *Prochaine étape* : tenter une lecture de bloc réelle (`i2ctransfer`) à l'adresse `0x81` (état de la LED power), et vérifier que la somme de contrôle du bloc reçu est cohérente. Objectif : valider le protocole complet sous Linux, avant de l'exposer côté FreeBSD — pour isoler, le jour venu, un problème de protocole d'un problème de portage.

== Prochaines étapes

* *Fait le 20/07/2026* : issue de contact zVault publiée (https://github.com/zvaultio/Community/issues/37). Surveiller les réponses. Le ticket XigmaNAS sur `sourceforge.net/p/xigmanas/tickets/` reste volontairement différé, à envoyer une fois le pilote validé sur matériel (après le sprint 3).
* *Cloner l'eMMC interne (UGOS)* avant la phase 4, depuis un live Linux vers un disque externe *dédié* (voir `scripts/backup-nas.sh`). Précaution recommandée et non prérequis absolu, voir la note du 20/07 sur le firmware téléchargeable.
* *Vérifier, sur `ai.ugreen.com/pages/downloads`, ce qui est réellement proposé pour le DXP2800* : format du fichier, et si sa documentation décrit une réinstallation à nu depuis clé USB ou seulement une mise à jour depuis UGOS. Cette réponse détermine si le clone reste utile ou devient superflu.
* Réception du matériel réseau (pont GL-SFT1200 + KVM Comet GL-RM1) restant attendue ; ne bloque plus le premier démarrage externe, l'écran HDMI et le clavier USB directs y suffisant.
* Préparer la clé USB zVault et tenter le premier démarrage externe sur le DXP2800, en notant la touche réelle du menu de démarrage (`Ctrl-F12` annoncé, à confirmer) et en désactivant le watchdog dans le BIOS.
* Sprint 3 : premier test réel du protocole SMBus du contrôleur LED sur le DXP2800 sous FreeBSD/zVault.
* Trancher et commander le second NAS de test (`DXP2800` ou `DXP4800 Plus`, voir `docs/catalogue-nas-ugreen.adoc`) et, à l'occasion, deux disques SATA 2,5" pour le DXP2800 actuel.
* Depuis Claude Code (nouvelle session, en local) : commencer par lire `CLAUDE.md`, ce journal et le plan de projet avant de reprendre le travail.
